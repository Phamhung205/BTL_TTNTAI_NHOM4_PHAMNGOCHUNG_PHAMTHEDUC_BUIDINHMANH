# Thu vien can thiet cho du an.
# Cai bang lenh:  pip install -r requirements.txt
#
# Dung >= de linh hoat phien ban giua cac may. Neu muon khoa cung
# dung phien ban giong nhau het, doi >= thanh == .

tensorflow>=2.15
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
jupyter>=1.0
